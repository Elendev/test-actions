on:
    issues:
        types: [labeled]

jobs:
    release_prod:
        name: realease prod
        runs-on: ubuntu-20.04
        steps:
            - run: echo "Releasing in production"
            - run: echo "Close issue"
        if: contains(github.event.issue.labels.*.name, 'to-deploy')